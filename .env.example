# Backend API origin. Vite inlines this at build time.
# Copy to .env.local for local overrides.
#
# Production (Coolify / Docker default):
VITE_API_BASE_URL=https://api-aioffice.fjdresources.com
#
# Local full-stack (backend on :3001):
# VITE_API_BASE_URL=http://localhost:3001
