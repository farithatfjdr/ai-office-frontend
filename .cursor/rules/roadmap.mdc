---
description: Ordered next work for the AI Office frontend (and required backend)
alwaysApply: true
---

# Roadmap

Continue from the last unfinished item. Backend work lives in `fjdresources/ai-office-backend`; this repo only consumes APIs.

## Done

- Northgate UI scaffold (Vite + React + Tailwind)
- TOTP login, JWT session, live agent chat
- Docker / nginx / Coolify config
- Repo hygiene: `.gitignore`, `.env.example`, `.cursor/environment.json`, Cursor rules
- Persist messages + MFA secret from `TOTP_SECRET` (backend)
- Load chat history per agent/War Room in this UI
- GitHub work-log rule: Issues + committed PRs are the record (chats are not)
- Projects + tasks from live APIs (`GET`/`POST` `/api/projects` and `/api/tasks`); War Room create POSTs instead of local mocks
- Coolify Auto Deploy on `main` (GitHub App webhooks; Access Bypass on `/webhooks/*`)

## Next (in order)

1. **Unblock production login, then smoke-test** ([#6](https://github.com/fjdresources/ai-office-frontend/issues/6))
   - Not a UI bug; do not reopen #4. Login is **401 Invalid code**.
   - Coolify backend env leftover dual-stack names: [ai-office-backend#15](https://github.com/fjdresources/ai-office-backend/issues/15). Keep `TOTP_SECRET` (and `JWT_SECRET`, `OPENROUTER_API_KEY`, `CORS_ORIGIN`, `DB_PASSWORD`); delete `_PROD`/`_TEST` copies. Re-enroll Authenticator from `TOTP_SECRET` only (Base32 `A–Z` `2–7`; strip quotes and `=`). Do not use `JWT_SECRET`.
   - After login: Projects/Tasks (live API; empty is valid), create one of each from War Room, one agent message. One Issue per anomaly on the repo that will change. Do not mix files/activity into those.
2. **Files + activity** ([#5](https://github.com/fjdresources/ai-office-frontend/issues/5)) — next *feature* after #6
   - `GET /api/files` (upload/download) instead of hardcoded `FILES`
   - Activity feed endpoint instead of `INITIAL_ACTIVITY`

## Known low-priority polish

- Inline theme styles are duplicated; `T` in `constants.js` is the source of truth (see `shared.jsx`)
- Notifications remain local mock data
- Tools (Apify, n8n, web search) are listed in the UI but not wired
