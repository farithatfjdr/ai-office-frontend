---
description: Ordered next work for the AI Office frontend (and required backend)
alwaysApply: true
---

# Roadmap

Continue from the last unfinished item. Backend work lives in `fjdresources/ai-office-backend`; this repo only consumes APIs.

## Done

- Northgate UI scaffold (Vite + React + Tailwind)
- TOTP login, JWT session, live agent chat
- Docker / nginx / Coolify config
- Repo hygiene: `.gitignore`, `.env.example`, `.cursor/environment.json`, Cursor rules
- Persist messages + MFA secret from `TOTP_SECRET` (backend)
- Load chat history per agent/War Room in this UI
- GitHub work-log rule: Issues + committed PRs are the record (chats are not)
- Projects + tasks from live APIs (`GET`/`POST` `/api/projects` and `/api/tasks`); War Room create POSTs instead of local mocks
- Coolify Auto Deploy on `main` (GitHub App webhooks; Access Bypass on `/webhooks/*`)
- Production serves current `main`, verified by build-hash parity (see `git-deploy.mdc`)

## Next (in order)

Each item is a GitHub Issue on [fjdr-ai-office](https://github.com/orgs/fjdresources/projects/1). Issues are the todo list; this is the ordering.

1. **Files + activity** — Issue #5
   - `GET /api/files` (upload/download) instead of hardcoded `FILES`
   - Activity feed endpoint instead of `INITIAL_ACTIVITY`
   - `FILES` is read in both `Views.jsx` and `RightPanel.jsx`, and `FilesView` pads it with three more hardcoded names. All three go.
2. **Frontend Coolify smoke-test** — Issue #6
   - ~~Confirm https://ai-office.fjdresources.com serves this build~~ done at `e13482f`
   - Remaining steps all need the TOTP code, so a human or a session with the secret has to run them
   - Login, open Projects/Tasks (not the old 5/8 mocks), create one of each from War Room
   - One agent message against production API
3. **Parse `TASK:` lines from agent replies into task rows** — Issue #9

## Known low-priority polish

- Inline theme styles are duplicated; `T` in `constants.js` is the source of truth (see `shared.jsx`)
- Notifications remain local mock data — Issue #7 (`NOTIFICATIONS`, read by `TopBar.jsx`)
- Tools (Apify, n8n, web search) are listed in the UI but not wired — Issue #8 (`TOOLS`, read by `RightPanel.jsx`)
