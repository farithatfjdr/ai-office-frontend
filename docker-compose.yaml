version: '3.9'

services:
  ai-office-frontend:
    build:
      context: .
      args:
        VITE_API_BASE_URL: ${VITE_API_BASE_URL:-https://api-aioffice.fjdresources.com}
    expose:
      - "80"
    restart: unless-stopped
